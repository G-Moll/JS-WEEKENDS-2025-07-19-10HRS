/*
01 ¿Qué característica de JavaScript permite cambiar su comportamiento en tiempo de ejecución?
    - a. Estático
    - b. Compilado
    - c. Dinámico (*)
    - d. Interpretado
*/
var sample = 10;
console.log( sample, typeof sample );

sample = { name: "Peter" };
console.log( sample, typeof sample );

sample = [ "Joshua" ];
console.log( sample, typeof sample );
