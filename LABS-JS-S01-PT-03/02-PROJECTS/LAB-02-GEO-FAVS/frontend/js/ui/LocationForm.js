class LocationForm {

    constructor() {}

    static init() {
        console.log( "Starting LocationForm..." );
        // LocationForm.reset();
    }

    static getFormData( userData ) {
    }

    static async sendFormData( e, payloadData ) {
        e.preventDefault();
    }

}

export { LocationForm };
